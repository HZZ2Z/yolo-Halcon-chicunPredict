# 运行说明（生产版）

> 当前工程使用 HALCON 标定链路。运行前需要准备 HALCON 与 Hikrobot MVS 环境。
> 程序使用 HALCON 点级 `pixelToWorld()` 做毫米换算，不做整帧 world-plane 重采样；检测、显示仍保持原图坐标系。

## 命令行快速运行

在项目根目录执行：

```bash
bash scripts/run_demo.sh
```

如果 `build/metal_metrology` 不存在，脚本会自动编译。若当前二进制不是按 `USE_ONNXRUNTIME=ON` 构建，程序会在 ONNX 初始化时报错退出，此时请使用 `--rebuild`。

## 桌面端运行

桌面端包含工业科技风登录界面、实时测量控制台、历史记录查询和本地数据存储：

```bash
bash scripts/run_desktop.sh --rebuild
```

首次启动会创建管理员账号。之后测量记录保存在 `data/metrology.db`。历史页按“一次运行”为一条记录，双击记录可以查看该次运行的逐帧测量信息；历史页支持删除选中记录、批量删除和一键清空。桌面端界面使用 Qt 本地绘制动态背景和数据卡片，不依赖外部图片资源。

如果脚本提示没有生成 `metal_metrology_desktop`，说明系统缺少 Qt Widgets/Sql 开发包；安装 Qt 开发包后重新运行即可。

## 安装桌面图标并双击启动

确认桌面端已构建后执行：

```bash
bash scripts/install_desktop_launcher.sh
```

安装完成后，桌面和应用菜单会出现 `Metal Metrology` 启动入口，之后可以直接双击打开系统，不需要 IDE 或命令行。

如果尚未构建桌面端，先执行：

```bash
bash scripts/run_desktop.sh --rebuild
```

## 需要重编译时

```bash
bash scripts/run_demo.sh --rebuild
```

## 指定配置文件运行

```bash
bash scripts/run_demo.sh --config config/system.yaml
```

## HALCON + MVS 手动启动（排障用）

请逐行执行：

```bash
export HALCONROOT=/home/hzwang/MVTec/HALCON-25.11-Progress
export HALCONARCH=x64-linux
export LD_LIBRARY_PATH=$HALCONROOT/lib/$HALCONARCH:$LD_LIBRARY_PATH
source /opt/MVS/bin/set_env_path.sh
./build/metal_metrology config/system.yaml
```

## 常见现象

- 看见 `CUDA Execution Provider 已启用`，说明 ONNX GPU 推理已正常启用。
- 看见 `[HALCON] 使用标定文件: ...`，说明 HALCON 标定文件已被加载。
- 看见 `[CALIB] local scale ...`，说明系统正在输出中心、边缘和角落的局部毫米比例诊断。
- 窗口中的 `sigma=` 是多扫描线测量经误差传播得到的毫米标准差估计。
- 窗口中的 `scans=` 是当前有效参与聚合的扫描线数量，正常情况下应接近配置项 `multi_scan_count`。
- 如果窗口显示 `LOW QUALITY`，说明当前帧的 `sigma/scans/跳变` 没通过质量阈值；该帧不会进入最终平滑历史。
- 如果 Ubuntu 窗口只能看到上半部分，调小 `config/system.yaml` 中的 `display_max_height`，例如 `700` 或 `650`；程序会保持图像比例自动缩放。

## 排障

相机打开失败时先执行：

```bash
bash scripts/check_camera.sh
```

如果提示 `strict_calibration=1` 相关报错，说明当前 `.cal + 位姿.dat` 与现场安装姿态不匹配，需要重新检查标定文件。

如果提示 `HALCON error #2042: Feature has expired`，说明 HALCON 授权功能过期，需先恢复授权；重新编译不会改变这个错误。

## 当前交付边界

当前版本为生产运行版。尺寸精度主要依赖：

- HALCON 相机内参和位姿标定。
- 边缘点级世界平面映射。
- 数值雅可比误差传播。
- 多扫描线亚像素边缘定位。
- Huber 鲁棒聚合与 `sigma/scans` 质量控制。

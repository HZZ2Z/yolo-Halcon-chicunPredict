# 运行说明（可直接点开）

> 当前工程已切到 HALCON 标定链路。运行前需要同时准备 HALCON 与 MVS 环境变量。

## 推荐（快速模式）
在项目根目录执行：

```bash
bash scripts/run_demo.sh
```

## HALCON + MVS 手动启动（推荐排障）

请逐行执行（不要复制任何 `[]()` 链接格式）：

```bash
export HALCONROOT=/home/hzwang/MVTec/HALCON-25.11-Progress
export HALCONARCH=x64-linux
export LD_LIBRARY_PATH=$HALCONROOT/lib/$HALCONARCH:$LD_LIBRARY_PATH
source /opt/MVS/bin/set_env_path.sh
./build/metal_metrology config/system.yaml
```

## 需要重编译时

```bash
bash scripts/run_demo.sh --rebuild
```

## 指定配置文件运行

```bash
bash scripts/run_demo.sh --config config/system.yaml
```

## 直接运行可执行文件（手动方式）

```bash
source /opt/MVS/bin/set_env_path.sh && ./build/metal_metrology config/system.yaml
```

## 常见现象
- 如果看见 `CUDA Execution Provider 已启用`，说明 ONNX GPU 推理已正常启用。
- 如果看见 `[HALCON] 使用标定文件: ...`，说明 HALCON 标定文件已被加载。
- 如果提示相机打开失败，先确认相机连线和供电，再执行：

```bash
bash scripts/check_camera.sh
```

- 如果提示 `strict_calibration=1` 相关报错，说明当前 `.cal + 位姿.dat` 与现场安装姿态不匹配，需要重标定位姿。

# 项目架构分层说明

本文档用于说明工业金属零件视觉测量系统的工程结构、模块职责和数据流向。它不作为绘图工具的输入模板，而是面向项目维护、论文撰写和后续功能扩展的架构说明。系统的核心思想是：命令行程序和 Qt 桌面端共用同一套视觉测量核心，检测模块只负责目标粗定位，最终尺寸由亚像素边缘点经过 HALCON 世界平面映射和鲁棒聚合得到。

---

## 1. 架构说明是否需要保留

建议保留 `docs/architecture.md`，但不需要写得太复杂。README 更适合给第一次打开仓库的人快速了解项目，系统分析报告适合保存完整技术细节，而架构说明的作用是解释“代码如何组织”和“模块之间如何协作”。

如果去掉架构说明，后续维护者只能从 README 和长篇系统分析报告里推断代码结构，不利于快速理解项目。更合适的做法是保留一份简洁版架构文档，重点说明分层、模块职责、数据流和依赖边界，不写过多绘图格式和工具化内容。

---

## 2. 系统总体架构

本系统可以理解为一个“采集—检测—跟踪—测量—标定换算—质量控制—显示存储”的流水线。图像首先由工业相机采集进入系统，随后 YOLO-OBB 模型识别零件的位置和角度。由于检测框会存在一定抖动，系统使用 EKF 对目标中心和角度进行平滑。稳定后的目标框不是最终测量结果，而是用于指导多扫描线卡尺生成测量位置。

在精密测量阶段，系统围绕稳定 OBB 生成多条平行扫描线，在原始图像中提取左右亚像素边缘点。随后每个边缘点通过 HALCON 标定模型映射到世界平面坐标中，并在世界平面上进行 Huber 鲁棒聚合，得到毫米尺寸。系统还会根据边缘点协方差和 HALCON 映射的数值雅可比估计 sigma，用于判断当前帧测量结果是否可靠。

整体链路可以概括为：

```text
工业相机采集
→ YOLO-OBB 粗定位
→ EKF 稳定目标位姿
→ 多扫描线亚像素边缘提取
→ HALCON pixelToWorld 世界映射
→ Huber 鲁棒聚合
→ sigma / scans / jump 质量控制
→ OpenCV 或 Qt 显示
→ SQLite 存储与 CSV 导出
```

---

## 3. 架构分层

### 3.1 运行资产与外部依赖层

这一层包含系统运行所依赖的配置文件、模型文件、标定文件和第三方库。它们不属于业务代码，但决定了系统是否能正常运行。

主要内容包括：

```text
config/system.yaml
config/classes.txt
models/best_opset12.onnx
camera/相机内参.cal
camera/相机位姿.dat
Hikrobot MVS SDK
HALCON
ONNX Runtime CUDA
OpenCV
Qt Widgets / Qt Sql
```

其中，`config/system.yaml` 是系统主配置文件，控制输入源、模型路径、标定路径、采集分辨率、推理分频、测量分频、卡尺参数和质量阈值。ONNX 模型负责目标粗定位，HALCON 标定文件负责像素点到世界平面的毫米坐标映射。

这一层最容易出现的问题是环境不一致。例如 HALCON 授权过期、MVS SDK 未加载、ONNX Runtime GPU 版本不匹配、OpenCV 路径找不到等，都会导致系统无法启动或无法达到实时性能要求。

---

### 3.2 入口层

入口层负责启动系统。当前系统有两个入口：命令行入口和 Qt 桌面端入口。

命令行入口主要用于开发调试、现场排障和简化运行。它读取配置后直接启动测量 pipeline，并通过 OpenCV 窗口显示结果。桌面端入口则用于更完整的生产交互场景，包含登录、实时测量、历史查询、数据记录和 CSV 导出。

两种入口不应各自实现一套测量逻辑。它们的关系应理解为：入口不同，核心 pipeline 相同。命令行程序和 Qt 桌面端都调用同一套采集、推理、跟踪、测量、标定和质量控制模块。这样可以避免后续维护时出现“命令行结果和桌面端结果不一致”的问题。

相关文件主要包括：

```text
src/main.cpp
src/desktop/main_desktop.cpp
src/desktop/main_window.cpp
```

---

### 3.3 应用编排层

应用编排层负责把各个功能模块组织成完整的运行流程。它并不直接实现具体算法，而是负责初始化顺序、线程创建、队列管理、单帧处理调度和退出收敛。

系统启动后，应用编排层会先加载配置，再初始化 HALCON 标定、相机、ONNX 推理器、EKF 跟踪器、亚像素卡尺和可视化模块。初始化完成后，程序进入生产者/消费者结构：采集线程不断从相机读取图像并写入队列，处理线程从队列中取出图像并执行单帧处理。

相关文件主要包括：

```text
src/app/pipeline_init.cpp
src/app/pipeline_runner.cpp
src/app/pipeline_context.cpp
src/app/frame_processor.cpp
```

这一层的核心职责是让系统“跑起来”，并保证采集、处理和显示之间不会互相强耦合。`ThreadSafeQueue` 是这里的重要结构，它把相机采集和图像处理解耦，使相机采集不会因为某一帧处理较慢而直接阻塞整个程序。

---

### 3.4 视觉感知层

视觉感知层负责从图像中找到被测零件，并输出稳定的目标位置和姿态。它主要包含相机采集、YOLO-OBB 推理、坐标恢复、旋转框去重和 EKF 跟踪。

图像进入模型前会进行 letterbox 预处理，即按比例缩放并填充到模型输入尺寸。模型输出后，检测框必须从模型输入坐标恢复到原始图像坐标。这个步骤非常重要，因为后续 EKF、扫描线生成、边缘提取和显示都基于原图坐标系。如果遗漏坐标反变换，测量位置就会发生偏移。

YOLO-OBB 的输出只用于定位目标中心、宽高、角度、类别和置信度。系统并不直接把检测框宽高作为最终尺寸。检测结果经过 Rotated NMS 去重后送入 EKF，EKF 对目标中心和角度进行平滑，并在非推理帧上提供短时预测。

相关文件主要包括：

```text
src/camera_provider.cpp
src/onnx_inferencer.cpp
src/tracker_ekf.cpp
```

这一层的输出是稳定的 OBB，它为后续精密测量提供目标姿态和扫描线生成依据。

---

### 3.5 精密测量层

精密测量层是系统测量精度的核心来源。它不依赖检测框宽高直接计算尺寸，而是在稳定 OBB 的基础上生成多条扫描线，并在原始图像中提取真实边缘点。

系统根据 OBB 计算测量方向和扫描线偏移方向。当前默认测量长边，因此扫描线沿目标长边方向进行采样，并在法向上生成多条平行线。每条扫描线会采样一维灰度 profile，进行高斯平滑和梯度计算，再通过梯度加权 refinement 获得亚像素边缘位置。

多扫描线设计可以降低局部反光、阴影、污渍和边缘缺口对结果的影响。单条扫描线如果异常，后续 Huber 聚合和质量控制可以降低它对最终结果的影响。

相关文件主要包括：

```text
src/subpixel_caliper.cpp
```

这一层的输出是左右亚像素边缘点、边缘点像素协方差和有效扫描线数量。它们仍然属于像素坐标系，还不是最终毫米尺寸。

---

### 3.6 标定与世界映射层

标定与世界映射层负责把像素边缘点转换到世界平面毫米坐标中。系统使用 HALCON 的相机内参和位姿文件完成这一过程。

当前系统不对整幅图像进行去畸变或 world-plane 重采样。这样做的原因是 YOLO 模型是在原始图像分布上训练的，整帧重采样可能改变图像分布并影响检测。同时，保持检测、显示和边缘提取都在原图坐标系中，也可以减少坐标系统切换带来的复杂度。

系统只在测量阶段对参与计算的边缘点执行 `pixelToWorld()`。也就是说，只有左右亚像素边缘点会被映射到世界平面，最终尺寸在世界坐标系中计算。

相关文件主要包括：

```text
src/calibration.cpp
```

需要注意，系统启动时输出的局部比例诊断只用于检查标定映射是否基本合理，不是最终测量使用的固定比例尺。最终毫米尺寸不是由固定 `mm/px` 计算得到，而是由每个边缘点逐点映射到世界平面后聚合得到。

---

### 3.7 误差传播与质量控制层

误差传播与质量控制层用于判断当前帧测量是否可靠。系统使用数值雅可比估计像素坐标到世界坐标的局部映射关系，并将像素边缘点协方差传播到世界平面，得到 `sigma_mm`。这个值表示算法内部估计的毫米测量不确定度。

质量控制主要依据三个方面：有效扫描线数量、sigma 大小和帧间跳变。如果有效扫描线数量太少，说明边缘提取不充分；如果 sigma 过大，说明当前帧不确定度较高；如果当前测量值相对上一稳定值跳变过大，说明可能出现检测抖动、边缘误检或零件移动。

OK 帧会进入 5 帧中值平滑并更新稳定结果。LOW QUALITY 帧会显示原因，也可以作为低质量明细保存，但不会进入最终平滑历史，也不应参与 OK 均值统计。

相关文件主要包括：

```text
src/measurement_uncertainty.cpp
src/app/frame_processor.cpp
```

这一层体现了系统的工程可靠性。它让系统不是简单输出一个数值，而是能说明这个数值是否可信。

---

### 3.8 显示、存储与导出层

显示、存储与导出层负责把测量结果呈现给用户，并保存历史数据。命令行入口主要通过 OpenCV 窗口显示图像、OBB、边缘点和测量信息。Qt 桌面端则提供更完整的生产界面，包括登录、实时测量、历史记录、逐帧明细、删除记录和 CSV 导出。

桌面端使用 SQLite 保存本地数据。一次开始到停止测量形成一个批次，批次下保存逐帧测量明细。OK 帧和 LOW QUALITY 帧都可以作为明细保存，但批次均值统计应只基于 OK 帧。

相关文件主要包括：

```text
src/app/frame_visualizer.cpp
src/desktop/main_window.cpp
src/desktop/measurement_database.cpp
```

CSV 导出属于历史记录功能，不是每帧实时处理流程的一部分。绘制流程图或写论文时，不应把 CSV 导出画成每帧都执行的节点。

---

## 4. 关键数据流

系统中的主要数据流可以概括为：

```text
FrameData
→ OBBResult
→ MeasurementResult
→ StoredMeasurement / StoredRunSession
```

`FrameData` 表示相机采集到的一帧图像。`OBBResult` 表示 YOLO-OBB 检测或 EKF 跟踪后的旋转框。`MeasurementResult` 表示当前帧的测量结果，包括像素距离、毫米距离、sigma、有效扫描线数量和质量状态。桌面端会将 `MeasurementResult` 转换为数据库中的 `StoredMeasurement`，并按运行批次汇总为 `StoredRunSession`。

这条数据流体现了系统从图像到结果的逐步抽象过程：图像首先变成目标位置，目标位置再指导边缘测量，边缘测量再转换成毫米尺寸，最终被显示和保存。

---

## 5. 依赖关系原则

项目后续维护时，建议保持以下依赖原则。

领域算法模块不应依赖 Qt 桌面端，也不应依赖 `src/app` 中的编排逻辑。这样可以保证算法模块能够被命令行程序、桌面端或后续测试程序复用。

可视化模块只负责绘制结果，不应直接触发相机采集、模型推理或测量计算。数据库模块只负责保存和查询结果，不应参与测量算法判断。

CalibrationMapper 只负责标定读取和像素到世界平面的映射，不应承担目标检测或质量判断。SubpixelCaliper 只负责像素域边缘点提取，不应直接负责数据库存储或 Qt 显示。质量控制可以先保留在 `frame_processor.cpp` 中，后续如果逻辑继续复杂，可以单独拆成 `QualityGate` 模块。

这样的依赖关系可以让系统结构更稳定，也方便后续增加目标丢失保护、残差补偿、多类型零件适配和单元测试。

---

## 6. 当前架构的优点

当前架构的主要优点是主链路比较清晰。系统将目标检测、边缘测量、标定换算和结果显示分成了相对独立的模块，便于论文说明，也便于后续维护。

第二个优点是命令行和 Qt 桌面端复用了同一套测量核心。这样既能保留轻量级排障入口，也能提供面向生产使用的桌面界面。

第三个优点是测量链路具有较强的可解释性。YOLO-OBB 负责定位，SubpixelCaliper 负责边缘点，HALCON 负责世界映射，Huber 聚合负责抑制异常扫描线，sigma 和质量门控负责判断结果是否可靠。这种结构比单纯端到端输出尺寸更容易解释，也更适合毕业设计论文。

---

## 7. 当前架构仍需优化的地方

当前架构还有一些可以继续完善的地方。首先是目标丢失保护还不够独立。系统在非推理帧或短时无检测时会使用 EKF 预测，这对分频推理是必要的，但如果目标长时间离开视野，仍应增加连续丢失计数，并在超过阈值后标记 `NO_TARGET`，停止测量。

其次，质量控制逻辑目前主要集中在帧处理层。对于当前项目规模，这样是可以接受的；但如果后续增加更多质量原因，例如边缘强度不足、左右边缘不对称、目标角度异常、标定映射异常等，建议拆出独立的 QualityGate 模块。

第三，标定残差补偿还没有形成独立模块。当前系统依赖 HALCON 标定进行点级世界映射，如果后续通过标准件实验发现图像边缘区域仍有系统误差，可以在世界平面聚合后增加残差补偿模块。

第四，项目目前缺少完整单元测试和回归测试。后续可以针对配置解析、letterbox 坐标恢复、Huber 聚合、质量判断和数据库导出等模块增加测试，提升长期维护稳定性。

---

## 8. 建议保留的架构说明粒度

这份架构说明不需要写成系统分析报告那样长，也不需要写成绘图工具的节点表。它只需要让读者明白三件事：系统分成哪些层，每一层做什么，各层之间如何传递数据。

如果后续继续精简，可以保留以下核心内容：

```text
1. 系统总体架构
2. 架构分层
3. 关键数据流
4. 依赖关系原则
5. 当前优点与后续优化方向
```

这样既不会和 README 重复太多，也不会和系统分析报告冲突。
